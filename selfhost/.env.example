# 复制为 .env 再填值
#
# HANDY_MASTER_SECRET：服务端用来签 token、做加密的主密钥
# 生成命令：
#   openssl rand -hex 32
# 一旦设置，绝对不要更改 —— 改了之后所有已注册用户都会失效
HANDY_MASTER_SECRET=

# 镜像版本。不填默认 latest；想锁版本填 v0.1.0 这种
# 升级流程：改这一行 -> docker compose pull -> docker compose up -d
HAPPY_SERVER_TAG=latest
